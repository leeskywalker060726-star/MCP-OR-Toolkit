{"name": "solve_linear_programming", "exact_algo_type": "LP", "description": "Solve pure continuous Linear Programming (LP) problems. Use this when variables can be fractions (e.g., liters of water, kg of flour).", "inputSchema": {"type": "object", "properties": {"payload": {"type": "object", "description": "Mathematical model definition containing 'sense' (MIN/MAX), 'objective' (dict), and 'constraints' (list of dicts with terms, op, rhs)."}}, "required": ["payload"]}}
{"name": "solve_mixed_integer_programming", "exact_algo_type": "ILP_BB", "description": "Solve Mixed-Integer Linear Programming (MILP) using Branch and Bound. Use this when SOME or ALL variables MUST be integers (e.g., number of people, cars). You MUST declare integer variables in the 'integer_variables' array.", "inputSchema": {"type": "object", "properties": {"payload": {"type": "object", "description": "Model containing 'sense', 'objective', 'constraints', and 'integer_variables' (array of strings for variables that must be whole numbers)."}}, "required": ["payload"]}}
{"name": "solve_pure_integer_programming", "exact_algo_type": "GOMORY", "description": "Solve Pure Integer Linear Programming (ILP) using Gomory Cutting Planes. Use this ONLY when ALL variables in the problem are strictly integers.", "inputSchema": {"type": "object", "properties": {"payload": {"type": "object", "description": "Model containing 'sense', 'objective', and 'constraints'. No need to specify integer_variables as all are assumed integer."}}, "required": ["payload"]}}
{"name": "solve_unweighted_bipartite_matching", "exact_algo_type": "HUNGARIAN", "description": "Solve unweighted 1-to-1 matching (Assignment Problem) using the Hungarian algorithm. Use this to maximize the number of matches between two groups without considering profit/cost.", "inputSchema": {"type": "object", "properties": {"payload": {"type": "object", "description": "Must contain 'edges', a list of dicts with 'u' (left node) and 'v' (right node)."}}, "required": ["payload"]}}
{"name": "solve_maximum_weight_matching", "exact_algo_type": "KM", "description": "Solve maximum weight perfect bipartite matching using the Kuhn-Munkres (KM) algorithm. Use this to assign tasks to workers to maximize TOTAL PROFIT or EFFICIENCY.", "inputSchema": {"type": "object", "properties": {"payload": {"type": "object", "description": "Must contain 'edges', a list of dicts with 'u' (left node), 'v' (right node), and 'w' (integer weight/profit)."}}, "required": ["payload"]}}
{"name": "solve_epsilon_greedy_bandit", "exact_algo_type": "EPSILON_GREEDY", "description": "Solve Multi-Armed Bandit (MAB) using Epsilon-Greedy strategy. Use this for simple baseline exploration with a fixed probability. Optional hyperparameter 'epsilon' (default 0.1) controls the exploration rate.", "inputSchema": {"type": "object", "properties": {"payload": {"type": "object", "properties": {"arms": {"type": "object", "description": "Dictionary of arms. Key is arm name, value is object with 'pulls' (int) and 'total_reward' (float)."}, "hyperparameters": {"type": "object", "description": "Optional hyperparams: 'epsilon' (float) and 'seed' (int)."}}, "required": ["arms"]}}, "required": ["payload"]}}
{"name": "solve_ucb_bandit", "exact_algo_type": "UCB", "description": "Solve Multi-Armed Bandit (MAB) using Upper Confidence Bound (UCB). Use this for rigorous, optimistic exploration mathematically guaranteed to minimize regret. Optional hyperparameter 'c' (default 1.414) controls exploration bounds.", "inputSchema": {"type": "object", "properties": {"payload": {"type": "object", "properties": {"arms": {"type": "object", "description": "Dictionary of arms. Key is arm name, value is object with 'pulls' (int) and 'total_reward' (float)."}, "hyperparameters": {"type": "object", "description": "Optional hyperparams: 'c' (float) and 'seed' (int)."}}, "required": ["arms"]}}, "required": ["payload"]}}
{"name": "solve_thompson_sampling_bandit", "exact_algo_type": "THOMPSON_SAMPLING", "description": "Solve Multi-Armed Bandit (MAB) using Thompson Sampling (Bayesian Beta distribution). Use this for the most robust performance in balancing exploration and exploitation in real-world scenarios. Optional hyperparameters 'alpha0' and 'beta0' (default 1.0) set the prior distribution.", "inputSchema": {"type": "object", "properties": {"payload": {"type": "object", "properties": {"arms": {"type": "object", "description": "Dictionary of arms. Key is arm name, value is object with 'pulls' (int) and 'total_reward' (float)."}, "hyperparameters": {"type": "object", "description": "Optional hyperparams: 'alpha0', 'beta0' (floats) and 'seed' (int)."}}, "required": ["arms"]}}, "required": ["payload"]}}